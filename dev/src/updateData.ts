/**
 * @author Gavin Sykes <gavin@gavinsykes.uk> (https://gavinsykes.uk/) [@gavinsykes_uk](https://twitter.com/gavinsykes_uk)
 * @license MIT
 */

/**
 * updateData takes a JavaScript object as its argument. It updates the visualisation's data to match the new data.
 *
 * The return value is void.
 *
 * @param {Record<string, unknown>} data - the new data.
 *
 */
function updateData(data: Record<string, unknown>[] = []): Record<string, unknown>[] {
  return data;
  // The line above does nothing yet other than to shut the TypeScript compiler up
}

export default updateData;
